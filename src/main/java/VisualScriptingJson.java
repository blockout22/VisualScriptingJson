import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

public class VisualScriptingJson extends Plugin {
    public VisualScriptingJson(PluginWrapper wrapper) {
        super(wrapper);
        System.out.println("Json Loaded");
    }
}
